// Enum to represent the days of the week
typedef enum WeekDay
{
    Sunday = 0,
    Monday,
    Tuesday,
    Wednesday,
    Thursday,
    Friday,
    Saturday
} weekday;

// Struct to represent a day of the week
typedef struct Day
{
    weekday day_index;
} day;
